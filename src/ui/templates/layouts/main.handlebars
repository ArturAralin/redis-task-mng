<!DOCTYPE html>
<html lang="en" data-theme="light">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="{{serverPrefix}}/static/pico.min.css">
  <style>
    :root {
      --pico-font-size: 100%;
    }
  </style>
  <title>{{pageTitle}}</title>
</head>
<body>
  <main class="container">
    <nav>
      <ul>
        <li><strong>RTM</strong></li>
      </ul>
      <ul>
        <li><a href="{{serverPrefix}}/dashboard">Dashboard</a></li>
        <li><a href="{{serverPrefix}}/tasks">Tasks</a></li>
      </ul>
    </nav>
    {{{body}}}
    <div class="grid">
      <div></div>
      <div>
        <form method="post" action="{{serverPrefix}}/timezone">
          <fieldset>
            <label>
              Timezone
              <select name="timezone" aria-label="Select" required onchange="this.form.submit()">
              {{#each timezones}}
                <option value="{{offset}}" {{#if current}}selected{{/if}}>{{name}}</option>
              {{/each}}
              </select>
            </label>
          </fieldset>
        </form>
      </div>
      <div></div>
    </div>
  </main>
</body>
</html>
